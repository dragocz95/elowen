<system-reminder>
<workflow-mode>
## Workflow mode continues

The full workflow instructions remain active. Define self-contained nodes and dependencies, then run them with `WorkflowStart`; handle trivial work directly. Do not present a proposal or ask whether to proceed. Delivery is asynchronous by default: continue independent work and wait for the completion turn without polling. Request `background=false` only when you need to block for results.
</workflow-mode>
</system-reminder>
