import { domMax } from 'motion/react';

// Layout continuity and pointer gestures are part of Elowen's shared interaction language.
// This bundle is still loaded lazily by EffectsProvider, so the initial shell stays lean.
export default domMax;
