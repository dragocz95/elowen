import { Code2 } from 'lucide-react';
import type { ModuleMeta } from '../types';

export const meta: ModuleMeta = {
  id: 'editor',
  label: 'Editor',
  route: '/editor',
  icon: Code2,
  group: 'Operate',
};
