import { Rocket } from 'lucide-react';
import type { ModuleMeta } from '../types';

export const meta: ModuleMeta = {
  id: 'missions',
  label: 'Missions',
  route: '/missions',
  icon: Rocket,
  group: 'Operate',
};
