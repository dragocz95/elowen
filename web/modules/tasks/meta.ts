import { ListChecks } from 'lucide-react';
import type { ModuleMeta } from '../types';

export const meta: ModuleMeta = {
  id: 'tasks',
  label: 'Tasks',
  route: '/tasks',
  icon: ListChecks,
  group: 'Operate',
};
