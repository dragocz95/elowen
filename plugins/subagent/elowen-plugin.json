{
  "name": "subagent",
  "version": "0.1.0",
  "apiVersion": "1",
  "description": "Delegation: spin up a fresh isolated sub-agent to handle a self-contained task and return its result. The sub-agent inherits the caller's access, never more.",
  "entry": "index.mjs",
  "provides": {
    "tools": [
      "Delegate",
      "DelegateStatus",
      "DelegateResult",
      "DelegateModels",
      "DelegateList",
      "DelegateRead",
      "DelegateContinue",
      "DelegateStop",
      "WorkflowStart",
      "WorkflowAddNodes",
      "WorkflowStatus",
      "WorkflowResume",
      "WorkflowStop"
    ],
    "platforms": [
      "subagent"
    ],
    "apiRoutes": [
      "/plugins/agents/list",
      "/plugins/agents/:name"
    ]
  },
  "icons": {
    "Delegate": "🤝",
    "DelegateStatus": "⏳",
    "DelegateResult": "📥",
    "DelegateModels": "🤝",
    "DelegateList": "📋",
    "DelegateRead": "📖",
    "DelegateContinue": "✉️",
    "DelegateStop": "🛑",
    "WorkflowStart": "🔀",
    "WorkflowAddNodes": "➕",
    "WorkflowStatus": "📊",
    "WorkflowResume": "▶️",
    "WorkflowStop": "🛑"
  },
  "planSafe": [
    "DelegateStatus",
    "DelegateResult",
    "DelegateModels",
    "DelegateList",
    "DelegateRead",
    "WorkflowStatus"
  ],
  "configSchema": [
    {
      "key": "sec_retention",
      "label": "Retention",
      "type": "section",
      "hint": "How long a finished delegation stays collectable."
    },
    {
      "key": "resultRetentionMs",
      "label": "Result retention (ms)",
      "type": "number",
      "min": 600000,
      "max": 86400000,
      "step": 600000,
      "placeholder": "3600000",
      "default": 3600000,
      "hint": "How long after finishing a background delegation or a workflow can still be read with DelegateStatus, DelegateResult or WorkflowStatus. Past it the entry is forgotten and the id no longer resolves. Range 10 minutes to 24 hours, default 1 hour."
    },
    {
      "key": "sec_watchdog",
      "label": "Stall watchdog",
      "type": "section",
      "hint": "When a delegated sub-agent that has gone silent is aborted."
    },
    {
      "key": "stallMinutes",
      "label": "Stall timeout (minutes)",
      "type": "number",
      "min": 5,
      "max": 240,
      "step": 5,
      "placeholder": "60",
      "default": 60,
      "hint": "How long a delegated sub-agent may go with no sign of life (no tool starts, no step boundaries) before it is aborted as stalled. The verdict is recoverable with DelegateContinue, so it is only a safety net for a genuinely wedged child — raise it for deep analyses that think for long stretches. Range 5 minutes to 4 hours, default 1 hour."
    }
  ],
  "capabilities": {
    "reads": [
      "agent-catalog"
    ]
  },
  "web": {
    "entry": "web/index.js",
    "requiresApiVersion": 1,
    "settings": [
      {
        "id": "subagents",
        "label": "Sub-agents",
        "icon": "GitFork"
      }
    ],
    "strings": {
      "title": "Sub-agents",
      "sectionHint": "Typed sub-agents the assistant can delegate to. Built-in explore and plan ship read-only; add your own.",
      "empty": "No sub-agents yet.",
      "badgeBuiltin": "built-in",
      "badgeUser": "custom",
      "add": "Add sub-agent",
      "edit": "Edit sub-agent",
      "remove": "Delete sub-agent",
      "deleteTitle": "Delete sub-agent",
      "deleteDesc": "The sub-agent “{name}” will be removed permanently.",
      "name": "Name",
      "description": "Description",
      "tools": "Tools",
      "toolsHint": "What the sub-agent may use. Read-only grants tools that cannot edit, plus a shell limited to non-destructive commands.",
      "toolsReadOnly": "Read-only (+ non-destructive shell)",
      "toolsAll": "All (full toolset)",
      "toolsInherit": "Inherit the caller’s tools",
      "toolsCustom": "Custom list…",
      "customTools": "Tool list",
      "customToolsHint": "Comma-separated tool names, e.g. Read, Search, Bash.",
      "body": "Prompt",
      "bodyPlaceholder": "The sub-agent’s system prompt…",
      "save": "Save sub-agent",
      "cancel": "Cancel",
      "created": "Sub-agent saved",
      "updated": "Sub-agent updated",
      "deleted": "Sub-agent deleted",
      "helpName": "kebab-case: lowercase letters, digits and dashes (e.g. “code-reviewer”).",
      "helpDescription": "One line: when to delegate to this sub-agent (the assistant reads it to choose).",
      "helpBody": "The sub-agent’s system prompt — its role, constraints and how to report back."
    }
  }
}
