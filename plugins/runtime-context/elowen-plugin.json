{
  "name": "runtime-context",
  "version": "0.2.0",
  "apiVersion": "1",
  "description": "Injects the current date, time, weekday and timezone into every turn, plus who the sender is and whether this is a private chat or a shared room, so Elowen never guesses about \"now\" nor about whom it is acting for. Cache-safe: both ride the user message, not the system prompt.",
  "entry": "index.mjs",
  "provides": {},
  "configSchema": [
    { "key": "timezone", "label": "Timezone", "type": "string", "hint": "IANA timezone, e.g. Europe/Prague. Empty = the server's own timezone. This is YOUR timezone for the whole assistant: it sets the date/time injected into every turn AND the clock every scheduled job runs on, so \"daily 07:30\" fires at 07:30 where you are." }
  ]
}
