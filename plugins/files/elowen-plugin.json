{
  "name": "files",
  "version": "0.2.0",
  "apiVersion": "1",
  "description": "Read, write, and list files within the user's accessible Elowen repositories.",
  "entry": "index.mjs",
  "provides": {
    "tools": [
      "Read",
      "Write",
      "Edit",
      "ListDir",
      "Search",
      "FileInfo",
      "GitStatus",
      "Glob",
      "Grep"
    ]
  },
  "icons": {
    "Read": "📄",
    "ListDir": "📂",
    "Write": "✏️",
    "Edit": "✏️",
    "Search": "🔎",
    "FileInfo": "📄",
    "GitStatus": "🌿",
    "Glob": "🔎",
    "Grep": "🔎"
  },
  "planSafe": [
    "Read",
    "Search",
    "ListDir",
    "FileInfo",
    "GitStatus",
    "Glob",
    "Grep"
  ],
  "configSchema": [
    {
      "key": "sec_limits",
      "label": "Limits",
      "type": "section",
      "hint": "Output size caps for file reads and searches."
    },
    {
      "key": "readCap",
      "label": "Read cap",
      "type": "number",
      "min": 20000,
      "max": 500000,
      "step": 5000,
      "placeholder": "100000",
      "default": 100000,
      "hint": "Max bytes read from a file and returned before truncation."
    },
    {
      "key": "searchMaxMatches",
      "label": "Search max matches",
      "type": "number",
      "min": 50,
      "max": 1000,
      "step": 10,
      "placeholder": "200",
      "default": 200,
      "hint": "Max number of matches Search returns before truncating."
    },
    {
      "key": "globMax",
      "label": "Glob max results",
      "type": "number",
      "min": 10,
      "max": 500,
      "step": 10,
      "placeholder": "100",
      "default": 100,
      "hint": "Max number of files Glob returns before truncating."
    },
    {
      "key": "pdfMaxPages",
      "label": "PDF pages per read",
      "type": "number",
      "min": 5,
      "max": 50,
      "step": 5,
      "placeholder": "20",
      "default": 20,
      "hint": "Maximum PDF pages one Read call may request. Raising this limit lets the model request wider ranges. Range 5–50; default 20."
    }
  ]
}
