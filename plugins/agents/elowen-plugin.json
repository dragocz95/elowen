{
  "name": "agents",
  "version": "0.1.0",
  "apiVersion": "1",
  "description": "Autonomous CLI agents and missions: spawns coding agents in tmux (or on embedded Elowen AI models), watches their progress, and runs the autopilot that plans, reviews and steers multi-task missions. Extracted from the Elowen core onto the plugin platform; disabling it turns off agent spawning and missions while chat, tasks and projects keep working.",
  "entry": "dist/index.js",
  "capabilities": {
    "reads": [
      "db",
      "tmux",
      "brain-worker",
      "elowen-cli",
      "stores",
      "prompts",
      "config",
      "inference",
      "git",
      "push",
      "terminals"
    ],
    "mutates": [
      "prompt",
      "events"
    ]
  },
  "provides": {
    "apiRoutes": [
      "/missions",
      "/sessions",
      "/tasks/:id/ask",
      "/tasks/:id/guide",
      "/asks/pending"
    ],
    "tools": [
      "ElowenListMissions",
      "ElowenListSessions"
    ]
  },
  "planSafe": [
    "ElowenListMissions",
    "ElowenListSessions"
  ],
  "configSchema": [
    {
      "key": "sec_overseer",
      "label": "Overseer",
      "type": "section",
      "hint": "Decision model for the autopilot overseer."
    },
    {
      "key": "overseerModel",
      "label": "Overseer model",
      "type": "string",
      "placeholder": "(planner model)",
      "default": "",
      "hint": "Relay model for overseer decisions; empty falls back to the planner model."
    },
    {
      "key": "sec_pr",
      "label": "PR-native missions",
      "type": "section",
      "hint": "Branch, auto-open and verify gate for mission pull requests."
    },
    {
      "key": "prBaseBranch",
      "label": "PR base branch",
      "type": "string",
      "placeholder": "(auto-detect)",
      "default": "",
      "hint": "Base branch mission PRs target; empty auto-detects per repository."
    },
    {
      "key": "prAutoOpen",
      "label": "Open PR automatically",
      "type": "boolean",
      "default": false,
      "hint": "Open a mission's PR as soon as its epic completes."
    },
    {
      "key": "prVerifyCommand",
      "label": "PR verify command",
      "type": "string",
      "placeholder": "(none)",
      "default": "",
      "hint": "Command that must exit 0 in the worktree before any PR opens; empty disables the gate."
    }
  ],
  "web": {
    "entry": "web/index.js",
    "requiresApiVersion": 1,
    "nav": [
      {
        "label": "Sessions",
        "icon": "SquareTerminal",
        "route": "sessions"
      },
      {
        "label": "Escalations",
        "icon": "ShieldAlert",
        "route": "escalations"
      }
    ],
    "settings": [
      {
        "id": "agents",
        "label": "Agents & Autopilot",
        "icon": "Bot"
      }
    ]
  }
}
