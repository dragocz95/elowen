{
  "name": "ui-demo",
  "version": "0.1.0",
  "apiVersion": "1",
  "description": "Reference plugin for the browser UI platform: a sample page served from a runtime bundle, backed by an authenticated plugin API route. Enable it to see how plugin pages, navigation and plugin APIs fit together — and as a template for writing your own.",
  "entry": "index.mjs",
  "provides": { "apiRoutes": ["stats"] },
  "web": {
    "entry": "web/index.js",
    "requiresApiVersion": 1,
    "nav": [{ "label": "UI demo", "icon": "Sparkles", "route": "" }]
  }
}
