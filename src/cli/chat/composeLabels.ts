/** CLI compatibility seam for browser-safe chat presentation rules shared with the web. */
export {
  DEFAULT_COMPOSE_MARKER_MS,
  DEFAULT_LONG_TOOL_COMPOSE_MARKER_MS,
  LONG_COMPOSE_TOOLS,
  composeLabel,
  composingLabel,
  type ComposeLocale,
} from '../../shared/chatPresentation.js';
